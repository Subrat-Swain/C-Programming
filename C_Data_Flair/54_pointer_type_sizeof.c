// Program for type of pointer size;
#include<stdio.h>
#include<conio.h>
void main()
    {
        int a = 100, *p;
        int s;
        s = sizeof(a);
        printf("\nSize is %d",s);
        printf("\nSize is %d",sizeof(p));
    }